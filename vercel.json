{
  "version": 2,
  "builds": [
    {
      "src": "portfolio_project/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.12",
        "buildCommand": "pip uninstall -y django-admin-interface django-colorfield || true && python -m pip install --upgrade pip setuptools wheel && python -m pip install -r requirements.txt && python manage.py migrate --noinput && python manage.py collectstatic --noinput"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "portfolio_project/wsgi.py"
    }
  ]
}
